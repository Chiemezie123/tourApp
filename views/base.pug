doctype html 
html 
    head
        block head
            title=`${title}`
            meta(charset="UTF-8")
            meta(name="viewport" content="width=device-width, initial-scale=1.0")
            link(rel='stylesheet', href='/css/style.css')
            link(rel='shortcut icon' type='image/png'  href='/img/favicon.png')
            link(rel='stylesheet', href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
            //- script(src='https://api.mapbox.com/mapbox-gl-js/v2.9.1/mapbox-gl.js')
    body
        //- the include operator is used in this case to import a child component (heaader) into the base component
        //- header
        include _header.pug
        //- the block in the case content is used to show where import content would replace when other route extends to the base pug
        block content
            h1 this is the landing page

        include _footer.pug

script(src='/js/bundle.js')








